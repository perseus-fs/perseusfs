{
  "name": "perseusfs",
  "version": "0.0.5",
  "scripts": {
    "build": "bun --bun ./scripts/build.ts"
  },
  "workspaces": [
    "server",
    "app",
    "shared",
    "scripts"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}